# Embeddings

Pluggable embedding providers. The store needs vectors — how you generate them is your business.

## EmbeddingProvider (base class)

::: pgmemory.embeddings.EmbeddingProvider

## VertexEmbeddingProvider

::: pgmemory.embeddings.VertexEmbeddingProvider

## OllamaEmbeddingProvider

::: pgmemory.embeddings.OllamaEmbeddingProvider

## OpenAIEmbeddingProvider

::: pgmemory.embeddings.OpenAIEmbeddingProvider
